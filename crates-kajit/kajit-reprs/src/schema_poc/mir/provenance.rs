// @generated by kajit-foundation::generate_repr_poc. Do not edit manually.

use super::ast::*;

pub trait HasProvenance {
    fn provenance(&self) -> Option<&Prov>;
}

impl HasProvenance for Block {
    fn provenance(&self) -> Option<&Prov> {
        Some(&self.prov)
    }
}

impl HasProvenance for ConstRef {
    fn provenance(&self) -> Option<&Prov> {
        match self {
            Self::Named { prov, .. } => Some(prov),
            Self::Value { prov, .. } => Some(prov),
        }
    }
}

impl HasProvenance for DataArgsLine {
    fn provenance(&self) -> Option<&Prov> {
        Some(&self.prov)
    }
}

impl HasProvenance for DataResultsLine {
    fn provenance(&self) -> Option<&Prov> {
        Some(&self.prov)
    }
}

impl HasProvenance for Edge {
    fn provenance(&self) -> Option<&Prov> {
        Some(&self.prov)
    }
}

impl HasProvenance for EdgeArg {
    fn provenance(&self) -> Option<&Prov> {
        match self {
            Self::Identity { prov, .. } => Some(prov),
            Self::Mapped { prov, .. } => Some(prov),
        }
    }
}

impl HasProvenance for FixedReg {
    fn provenance(&self) -> Option<&Prov> {
        match self {
            Self::AbiArg { prov, .. } => Some(prov),
            Self::AbiRet { prov, .. } => Some(prov),
            Self::HwReg { prov, .. } => Some(prov),
        }
    }
}

impl HasProvenance for Function {
    fn provenance(&self) -> Option<&Prov> {
        Some(&self.prov)
    }
}

impl HasProvenance for Inst {
    fn provenance(&self) -> Option<&Prov> {
        Some(&self.prov)
    }
}

impl HasProvenance for InstOp {
    fn provenance(&self) -> Option<&Prov> {
        match self {
            Self::BinOp { prov, .. } => Some(prov),
            Self::CallEffect { prov, .. } => Some(prov),
            Self::CallIntrinsic { prov, .. } => Some(prov),
            Self::CallLambda { prov, .. } => Some(prov),
            Self::CallPure { prov, .. } => Some(prov),
            Self::Const { prov, .. } => Some(prov),
            Self::Copy(value) => Some(&value.prov),
            Self::DataAddr { prov, .. } => Some(prov),
            Self::ExternAddr { prov, .. } => Some(prov),
            Self::LoadFromAddr { prov, .. } => Some(prov),
            Self::ReadFromSlot { prov, .. } => Some(prov),
            Self::StackAlloc { prov, .. } => Some(prov),
            Self::StoreToAddr { prov, .. } => Some(prov),
            Self::UnaryOp { prov, .. } => Some(prov),
            Self::WriteToSlot { prov, .. } => Some(prov),
        }
    }
}

impl HasProvenance for IntrinsicRef {
    fn provenance(&self) -> Option<&Prov> {
        match self {
            Self::Address { prov, .. } => Some(prov),
            Self::Named { prov, .. } => Some(prov),
        }
    }
}

impl HasProvenance for Operand {
    fn provenance(&self) -> Option<&Prov> {
        Some(&self.prov)
    }
}

impl HasProvenance for Program {
    fn provenance(&self) -> Option<&Prov> {
        Some(&self.prov)
    }
}

impl HasProvenance for Terminator {
    fn provenance(&self) -> Option<&Prov> {
        match self {
            Self::Branch { prov, .. } => Some(prov),
            Self::BranchIf { prov, .. } => Some(prov),
            Self::BranchIfZero { prov, .. } => Some(prov),
            Self::JumpTable { prov, .. } => Some(prov),
            Self::Return { prov, .. } => Some(prov),
        }
    }
}

impl HasProvenance for UnaryOp {
    fn provenance(&self) -> Option<&Prov> {
        match self {
            Self::SignExtend { prov, .. } => Some(prov),
        }
    }
}
