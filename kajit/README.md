# kajit

kajit is a Rust JIT using RVSDG for its IR.

It puts a focus on debuggability for now because, damn, it's hard to write a
JIT.

Large swathes of kajit are currently disabled/excluded from the
workspace since an underlying crate (kali) is being rebuilt.
